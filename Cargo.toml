[package]
name = "DeepLearning"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.20.1", features = ["wgpu"] }
macroquad = "0.4.14"
burn = { version = "0.20.1", features = ["wgpu", "autodiff", "train"] }
rand = "0.10.0"
